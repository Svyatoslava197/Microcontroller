#include <stdio.h>

int main() {
    printf("VS Code настроен правильно!\n");
    printf("Компилятор работает корректно!\n");
    
    // Простые вычисления для проверки
    int a = 10;
    int b = 5;
    int sum = a + b;
    
    printf("Результат вычисления: %d + %d = %d\n", a, b, sum);
    printf("Тест завершен успешно! 🎉\n");
    
    return 0;
}
